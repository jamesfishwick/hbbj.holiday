[build]
  publish = "out"
  command = "node scripts/generate-sitemap.mjs && pnpm run build && pnpm dlx next export"

  environment = { NODE_VERSION = "20.18.2" }

[[plugins]]
  package = "@netlify/plugin-nextjs"
